<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }} - {{ metadata.siteName }}</title>
    <link rel="stylesheet" href="/css/tufte.css">
    <link rel="stylesheet" href="/css/custom.css">
</head>
<body>
    <article>
        <nav>
            <a href="/">Home</a> |
            <a href="/blog">Blog</a>
        </nav>
        
        <div class="container">
            <div class="main-content">
                {{ content | safe }}
            </div>
            <div class="sidebar">
                {% if sidenotes %}
                    {% for note in sidenotes %}
                        <p><sup class="sidenote-number">{{ note.number }}</sup> {{ note.text }}</p>
                    {% endfor %}
                {% endif %}
            </div>
        </div>
    </article>
</body>
</html>
